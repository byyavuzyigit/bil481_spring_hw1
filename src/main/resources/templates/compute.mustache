<title>My application deployed automatically</title>
  <h1>Hello, World!</h1>
  <p>
    In the first two forms, enter a list of integers seperated by a newline character.<br>
    In the last two forms, enter String texts.<br>
    The method will generate the result string by iterating through both lists and appending the corresponding text as many the element in the list.
   <p>

    Result is {{result}}


  <form action="compute" method="post">
        <textarea name="input1" rows="10" cols="30"></textarea>
        <textarea name="input2" rows="10" cols="30"></textarea>
        <textarea name="input3" rows="10" cols="30"></textarea>
        <textarea name="input4" rows="10" cols="30"></textarea>
        <br>
        <input type="submit">
  </form>
